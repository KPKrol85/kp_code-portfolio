{
  "name": "construction-website-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Statyczna strona demo: remonty i wykończenia",
  "author": "KP_Code",
  "license": "UNLICENSED",
  "scripts": {
    "start": "npm run dev",
    "dev": "live-server --port=15500 --open=index.html --quiet",
    "format": "prettier -w \"**/*.{html,css,js,json,md}\"",
    "format:check": "prettier -c \"**/*.{html,css,js,json,md}\"",
    "build:css": "cssnano css/style.css css/style.min.css",
    "build": "npm run build:css && npm run format",
    "build:js": "terser js/script.js -o js/script.min.js -c -m"
  },
  "devDependencies": {
    "cssnano-cli": "^1.0.5",
    "live-server": "1.2.2",
    "prettier": "3.3.3"
  },
  "engines": {
    "node": ">=18"
  }
}
